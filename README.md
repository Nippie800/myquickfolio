# Quickfolio

A playful, project-focused portfolio built with Python and Flask.

## Development

Create and activate a virtual environment, then install the dependencies:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt